{
  "name": "@createrington/shared",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.9.3"
  }
}
